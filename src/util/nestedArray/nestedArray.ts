export function nestedIndexOf(hay: any[][], needle: any[]): number {
	let i = 0;

	hay.forEach((e: any) => {
		if (e === needle { return i; }
		i = i + 1;
	});

	return -1;
}

/*
Array.prototype.nestedIndexOf = (search: any[]) => {
	let i = 0;

	this.forEach((e: any) => {
		if (e === search) { return i; }
		i = i + 1;
	});

};
*/
